---
title: 'Field'
description: 'A form field component set with label, error handling and inset capabilities.'
---
import BaseSidebar from "@/layouts/BaseSidebar.astro";
import CodePreview from "@/components/CodePreview.astro";
import SamplePage from "@/components/SamplePage.astro";
import Prose from '@/components/Prose.astro';
import Section from '@/components/Section.astro';
import Field from '@/components/field/Field.astro';
import FieldLabel from '@/components/field/FieldLabel.astro';
import FieldError from '@/components/field/FieldError.astro';
import Fieldset from '@/components/fieldset/Fieldset.astro';
import Input from '@/components/input/Input.astro';

<BaseSidebar title={frontmatter.title} description={frontmatter.description}>

    <CodePreview code={`
        <div className="flex flex-col gap-4">
          <div className="max-w-sm">
            <Field>
              <FieldLabel>Username</FieldLabel>
              <Input type="text" /> 
            </Field>
          </div>

          <div className="max-w-sm">
            <Field>
              <FieldLabel>Password</FieldLabel>
              <Input type="text" /> 
              <FieldError error="Password must be at least 8 characters" />
            </Field>
          </div>
        </div>
    `}>
      <div class="flex flex-col gap-4">
        <div class="max-w-sm">
          <Field>
            <FieldLabel>Username</FieldLabel>
            <Input type="text" /> 
          </Field>
        </div>

        <div class="max-w-sm">
          <Field>
            <FieldLabel>Password</FieldLabel>
            <Input type="text" /> 
            <FieldError error="Password must be at least 8 characters" />
          </Field>
        </div>
      </div>
    </CodePreview>

    <Section title="Overview">
        The Field component set provides a suite of components for building form fields with consistent styling, labels, error handling, and inset content. These components are built on top of the `@base-ui-components/react` Field primitive.
    </Section>

    <Section title="Field">
        ### Description
        The root component that wraps the form field elements. It can automatically include a label via the `label` prop.

        ### Usage
        <SamplePage>
          <Fieldset>
            <Field label="Label as Prop">
              <Input type="text" />
            </Field>

            <Field>
              <FieldLabel>Standard Label</FieldLabel>
              <Input type="text" />
            </Field>
          </Fieldset>
        </SamplePage>

        ```tsx
        import { Field, Input } from "@crafted-ui/react";

        <Field label="Username">
          <Input type="text" />
        </Field>

        <Field>
          <FieldLabel>Username</FieldLabel>
          <Input type="text" />
        </Field>
        ```

        ### Props
        The `Field` component accepts all props from the `@base-ui-components/react` Field.Root component, plus:
        
        | Prop | Type | Description |
        | --- | --- | --- |
        | label | string \| ReactNode | Label content (automatically renders a FieldLabel component) |
        | className | string | Additional CSS class names |
        | children | ReactNode | Content of the field |
    </Section>

    <Section title="FieldLabel">
        ### Description
        A label component for the form field.

        ### Props
        The `FieldLabel` component accepts all props from the `@base-ui-components/react` Field.Label component, plus:
        
        | Prop | Type | Description |
        | --- | --- | --- |
        | className | string | Additional CSS class names |
        | children | ReactNode | Content of the label |
    </Section>

    <Section title="FieldError">
        ### Description
        Displays validation error messages. This is automatically included in the Field component.

        ### Props
        The `FieldError` component accepts all props from the `@base-ui-components/react` Field.Error component, plus:
        
        | Prop | Type | Description |
        | --- | --- | --- |
        | className | string | Additional CSS class names |
    </Section>

    <Section title="FieldInset">
        ### Description
        Adds inset content to the input field, such as units or extra information.

        ### Example
        ```tsx
        <Field>
          <FieldLabel>Amount</FieldLabel>
          <div className="relative">
            <input 
              type="number" 
              className="w-full rounded-md border border-neutral-300 px-3 py-1.5 text-sm shadow-sm" 
            />
            <FieldInset text="USD" position="right" />
          </div>
        </Field>
        ```

        ### Usage with Text
        ```tsx
        import { Field, FieldLabel, FieldInset } from "@crafted-ui/react";

        <Field>
          <FieldLabel>Price</FieldLabel>
          <div className="relative">
            <input 
              type="number" 
              className="w-full rounded-md border border-neutral-300 px-3 py-1.5 text-sm shadow-sm" 
            />
            <FieldInset text="$" position="left" />
          </div>
        </Field>
        ```

        ### Usage with Custom Content
        ```tsx
        import { Field, FieldLabel, FieldInset } from "@crafted-ui/react";

        <Field>
          <FieldLabel>Search</FieldLabel>
          <div className="relative">
            <input 
              type="text" 
              className="w-full rounded-md border border-neutral-300 px-3 py-1.5 text-sm shadow-sm" 
            />
            <FieldInset position="right">
              <svg className="h-4 w-4 text-neutral-500" /* SVG properties */ />
            </FieldInset>
          </div>
        </Field>
        ```

        ### Props
        | Prop | Type | Default | Description |
        | --- | --- | --- | --- |
        | text | string | undefined | Text to display in the inset |
        | position | "left" \| "right" | "right" | Position of the inset |
        | children | ReactNode | undefined | Custom content to display in the inset |
    </Section>

</BaseSidebar>