---
title: 'Design Cues'
description: 'The design system and variables that power Crafted UI components'
---
import BaseSidebar from "@layouts/BaseSidebar.astro";
import Section from "@components/Section.astro";
import Container from "@components/container/Container.astro";
import Card from "@components/card/Card.astro";
import CardHeader from "@components/card/CardHeader.astro";
import CardBody from "@components/card/CardBody.astro";
import ColorSample from "@/components/ColorSample.astro";

<BaseSidebar title={frontmatter.title} description={frontmatter.description}>

<Section title="Introduction">
Crafted UI is built on a set of consistent design principles and variables that help maintain a cohesive visual language across all components. This design system provides the foundation for creating intuitive, accessible, and aesthetically pleasing user interfaces.

Design cues are visual signals that guide users through interfaces, create recognizable patterns, and establish hierarchy. They work together to create a cohesive experience that is both visually appealing and functionally effective.
</Section>

<Section title="Design Tokens">
Design tokens are the building blocks of our design system. They represent the smallest visual design decisions, such as colors, spacing, typography, and more. By using tokens instead of hard-coded values, we maintain consistency across our components.

### Base Tokens

Crafted UI uses these fundamental tokens to define its visual language:

- **Colors**: Primary, secondary, accent, and semantic colors
- **Typography**: Font families, sizes, weights, and line heights
- **Spacing**: Consistent spacing scales for margins, padding, and gaps
- **Borders**: Border widths, styles, and radii
- **Shadows**: Elevation levels through shadows
- **Transitions**: Animation duration and easing functions
</Section>

<Section title="Using Design Variables">
In Crafted UI, you can access design variables through both CSS variables and Tailwind utility classes. This dual approach provides flexibility for different styling scenarios.

### CSS Variables

You can directly use CSS variables in your custom styles:

```css
.custom-element {
  background-color: var(--primary);
  color: var(--primary-foreground);
  border: 1px solid var(--border);
}
```

### Tailwind Classes

For a more declarative approach, use the semantic color classes in Tailwind:

```html
<div class="bg-primary text-primary-foreground border border-border">
  This element uses Crafted UI's design tokens
</div>
```

### Customizing Variables

To customize the design variables for your project, you can override the CSS variables in your global styles:

```css
:root {
  --primary: 240 100% 50%; /* Blue */
  --secondary: 280 100% 50%; /* Purple */
}
```
</Section>

<Section title="Sizing Defaults">
Crafted UI provides a set of default sizing variables that can be used to maintain consistent spacing and sizing across components. These variables come with reasonable defaults but can easily be customized for consistent alternative sizing.

| Variable Name | Description | Default Value |
| ------------- | ----------- | ------------- |
| `container-container` | Base container width | `80rem` |
| `container-navbar` | Navbar height | `4rem` |
| `container-sidebar` | Sidebar width | `12rem` |
</Section>

<Section title="Color System">
Colors are a critical aspect of our design system. Crafted UI uses a semantic color system that assigns meaning to colors beyond their visual appearance.

### Semantic Colors

Rather than using color names like "green" or "blue," we use functional names that describe the purpose:

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6 not-prose">
  <Card title="Background Colors" subtitle="Colors used for component and page backgrounds">
    <CardBody>
      <div className="space-y-3">
        <ColorSample
            color="background"
            bgColor="bg-background"
            textColor="text-foreground"
            usage="Base container elements"
        />
        <ColorSample
            color="muted"
            bgColor="bg-muted"
            textColor="text-muted-foreground"
            usage="Secondary container elements"
        />
        <ColorSample
            color="popover"
            bgColor="bg-popover"
            textColor="text-popover-foreground"
            usage="Floating UI elements"
        />
        <ColorSample
            color="card"
            bgColor="bg-card"
            textColor="text-card-foreground"
            usage="Used for content containers"
        />
      </div>
    </CardBody>
  </Card>

  <Card title="Foreground Colors" subtitle="Colors used for text and icons">
    <CardBody>
      <div className="space-y-3">
        <ColorSample
            color="foreground"
            bgColor="bg-background"
            textColor="text-foreground"
            usage="Default text color"
        />
        <ColorSample
            color="muted-foreground"
            bgColor="bg-background"
            textColor="text-muted-foreground"
            usage="Secondary text content"
        />
        <ColorSample
            color="popover"
            bgColor="bg-background"
            textColor="text-popover-foreground"
            usage="Text on popovers"
        />
        <ColorSample
            color="card"
            bgColor="bg-background"
            textColor="text-card-foreground"
            usage="Text on card elements"
        />
      </div>
    </CardBody>
  </Card>

  <Card title="Action Colors" subtitle="Colors used for interactive elements">
    <CardBody>
        <div className="space-y-3">
            <ColorSample
                color="primary"
                bgColor="bg-primary"
                textColor="text-primary-foreground"
                usage="Main action buttons"
            />
            <ColorSample
                color="primary-foreground"
                bgColor="bg-primary-foreground"
                textColor="text-primary"
                usage="Main highlighted text"
            />
            <ColorSample
                color="secondary"
                bgColor="bg-secondary"
                textColor="text-secondary-foreground"
                usage="Alternative buttons"
            />
            <ColorSample
                color="secondary-foreground"
                bgColor="bg-background"
                textColor="text-secondary-foreground"
                usage="Secondary highlighted text"
            />
        </div>
    </CardBody>
  </Card>

  <Card title="Accent & Destructive" subtitle="Colors used for highlights and warnings">
    <CardBody>
      <div className="space-y-3">
           <ColorSample
                color="accent"
                bgColor="bg-accent"
                textColor="text-accent-foreground"
                usage="Highlighted UI elements"
            />
            <ColorSample
                color="accent-foreground"
                bgColor="bg-background"
                textColor="text-accent-foreground"
                usage="Text on highlighted areas"
            />
            <ColorSample
                color="destructive"
                bgColor="bg-destructive"
                textColor="text-destructive-foreground"
                usage="Destructive actions"
            />
            <ColorSample
                color="destructive-foreground"
                bgColor="bg-background"
                textColor="text-destructive"
                usage="Destructive action text"
            />
      </div>
    </CardBody>
  </Card>

  <Card title="Borders & Inputs" subtitle="Colors used for boundaries and form elements">
    <CardBody>
      <div className="space-y-3">
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded bg-border"></div>
          <div>
            <div className="font-mono text-sm">--border</div>
            <div className="text-xs text-muted-foreground">Borders and dividers</div>
            <div className="text-xs text-primary mt-1">Component separators</div>
          </div>
        </div>
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded bg-input"></div>
          <div>
            <div className="font-mono text-sm">--input</div>
            <div className="text-xs text-muted-foreground">Form input borders</div>
            <div className="text-xs text-primary mt-1">Form field outlines</div>
          </div>
        </div>
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded bg-ring"></div>
          <div>
            <div className="font-mono text-sm">--ring</div>
            <div className="text-xs text-muted-foreground">Focus rings</div>
            <div className="text-xs text-primary mt-1">Keyboard focus indicator</div>
          </div>
        </div>
      </div>
    </CardBody>
  </Card>

  <Card title="Chart Colors" subtitle="Colors used for data visualization">
    <CardBody>
      <div className="space-y-3">
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded" style="background: hsl(var(--chart-1))"></div>
          <div>
            <div className="font-mono text-sm">--chart-1</div>
            <div className="text-xs text-muted-foreground">Primary chart color</div>
            <div className="text-xs text-primary mt-1">Main data series</div>
          </div>
        </div>
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded" style="background: hsl(var(--chart-2))"></div>
          <div>
            <div className="font-mono text-sm">--chart-2</div>
            <div className="text-xs text-muted-foreground">Secondary chart color</div>
            <div className="text-xs text-primary mt-1">Supporting data series</div>
          </div>
        </div>
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded" style="background: hsl(var(--chart-3))"></div>
          <div>
            <div className="font-mono text-sm">--chart-3</div>
            <div className="text-xs text-muted-foreground">Tertiary chart color</div>
            <div className="text-xs text-primary mt-1">Tertiary data series</div>
          </div>
        </div>
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded" style="background: hsl(var(--chart-4))"></div>
          <div>
            <div className="font-mono text-sm">--chart-4</div>
            <div className="text-xs text-muted-foreground">Quaternary chart color</div>
            <div className="text-xs text-primary mt-1">Additional data series</div>
          </div>
        </div>
      </div>
    </CardBody>
  </Card>
</div>
</Section>

<Section title="Color Themes">
Crafted UI supports both light and dark themes out of the box. The color variables automatically adapt to the current theme, ensuring consistent appearance and proper contrast in both modes.

### Light Theme
The light theme uses softer, more neutral colors with appropriate contrast for readability. Background colors are light with darker text.

### Dark Theme
The dark theme inverts the color relationships, using dark backgrounds with light text. It maintains the same semantic color relationships while providing a comfortable viewing experience in low-light environments.

<div className="p-4 rounded-md border border-border mt-6">
  <h4 className="font-medium mb-4">Theme Comparison</h4>
  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
    <div className="bg-white p-4 rounded-md border border-slate-200">
      <div className="text-sm font-medium mb-2 text-slate-900">Light Theme</div>
      <div className="space-y-2">
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 33% 100%)"></div>
          <div className="text-xs font-mono text-slate-700">--background</div>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 40% 34%)"></div>
          <div className="text-xs font-mono text-slate-700">--primary</div>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 4% 84%)"></div>
          <div className="text-xs font-mono text-slate-700">--secondary</div>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 80% 4%)"></div>
          <div className="text-xs font-mono text-slate-700">--foreground</div>
        </div>
      </div>
    </div>
    <div className="bg-slate-900 p-4 rounded-md border border-slate-800">
      <div className="text-sm font-medium mb-2 text-slate-100">Dark Theme</div>
      <div className="space-y-2">
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 50% 0%)"></div>
          <div className="text-xs font-mono text-slate-300">--background</div>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 40% 34%)"></div>
          <div className="text-xs font-mono text-slate-300">--primary</div>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 12% 17%)"></div>
          <div className="text-xs font-mono text-slate-300">--secondary</div>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded" style="background: hsl(121 16% 98%)"></div>
          <div className="text-xs font-mono text-slate-300">--foreground</div>
        </div>
      </div>
    </div>
  </div>
</div>
</Section>

</BaseSidebar>