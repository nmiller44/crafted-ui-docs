---
title: 'Button'
description: 'A button styled component for user interaction.'
---
import BaseSidebar from "../../../layouts/BaseSidebar.astro";
import SamplePage from "../../../components/SamplePage.astro";
import Button from "../../../components/Button.astro";

import Prose from '../../../components/Prose.astro';
import Section from '../../../components/Section.astro';

<BaseSidebar title={frontmatter.title} description={frontmatter.description}>

    <SamplePage>
        <div class="py-8 grid grid-cols-4 lg:grid-cols-6 gap-4">
            <Button>Button</Button>
            <Button color="red">Button</Button>
            <Button color="yellow">Button</Button>
            <Button color="blue">Button</Button>
            <Button color="indigo">Button</Button>
            <Button color="white">Button</Button>
        </div>
    </SamplePage>

    <Section title="Usage">
        ### Usage
        The `Button` component can be passed an optional `type` parameter to change the button style. The default style is `primary`.

        #### Default Usage
        ```astro
        <Button>Button</Button>
        ```

        #### Custom Usage
        ```astro
        <Button type="secondary">Button</Button>
        ```
    </Section>

</BaseSidebar>
