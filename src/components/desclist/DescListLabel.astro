---
import { classNames } from "@/utils/classNames";

export interface Props {
    class?: string;
}

const { class: className, ...props } = Astro.props;
---

<dt class={classNames("text-sm font-semibold text-foreground", className)} {...props}>
    <slot />
</dt>
