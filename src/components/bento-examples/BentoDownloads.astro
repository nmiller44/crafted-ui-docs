---
import { classNames } from "@utils/classNames"
import BentoCard from "../bento/BentoCard.astro";
import Metric from "../metric/Metric.astro";

interface Props {
    className?: string;
}

const { 
    className
 } = Astro.props;
---
<BentoCard className={classNames("space-y-12 p-6", className)}>
    <Metric
        title="Downloads"
        value="1,234,567"
        diff="+12%"
        dir="positive" />    
</BentoCard>