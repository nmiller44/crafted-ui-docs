---
import { classNames } from "@utils/classNames"

interface Props {
    title?: string;
    subtitle?: string;
    className?: string;
    subtitleClass?: string;
    wrapperClass?: string;
}

const { title, subtitle, className, subtitleClass, wrapperClass } = Astro.props;
---
<div class={classNames("space-y-1.5", wrapperClass)}>
    { !!title
        ? <h3 class={classNames("text-2xl font-bold tracking-tight", className)}>{ title }</h3>
        : <slot />
    }
    { !!subtitle && <p class={classNames("text-sm text-muted-foreground", subtitleClass)}>{ subtitle }</p> }
</div>