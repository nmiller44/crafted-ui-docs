---
import { classNames } from "../../utils/classNames";

export type Props = {
  className?: string;
  [key: string]: any;
};

const { className, ...props } = Astro.props;
---

<svg 
  class={classNames("size-5", className)} 
  xmlns="http://www.w3.org/2000/svg" 
  viewBox="0 0 24 24" 
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
  {...props}
>
  <path d="M5 12h14" />
</svg>
