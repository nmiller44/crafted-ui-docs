---
import { classNames } from "../utils/classNames"
import HeaderSection from "./HeaderSection.astro";
import Prose from "./Prose.astro";

interface Props {
    title?: string;
    subtitle?: string;
    className?: string;
    prose?: boolean;
}

const { title, subtitle, prose = true, className } = Astro.props;
---
<section class={classNames(
    "space-y-8",
    className
)}>
    { 
        !!title && (
        <HeaderSection title={title} subtitle={subtitle} />
        )
    }
    <div>
        {
            prose
            ?   (<Prose >
                    <slot />
                </Prose>)
            :   <slot />
        }
    </div>
</section>
